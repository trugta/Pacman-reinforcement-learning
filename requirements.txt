numpy>=1.23.0,<1.24.0
ale_py == 0.12.0
Gymnasium == 1.3.0

# Pinned below 2.11.0: this is the last TensorFlow release with native
# Windows GPU support (2.11+ dropped it; WSL2 or tensorflow-directml-plugin
# are the alternatives on Windows going forward).
# Requires Python 3.7-3.10 and CUDA 11.2 / cuDNN 8.1 for GPU acceleration -
# see https://www.tensorflow.org/install/source_windows#gpu
tensorflow>=2.10.0,<2.11.0
opencv-python>=4.6.0
Pillow>=9.1.0

# Optional: if you have GPU and want tensorflow with GPU support, install accordingly